CREATE TABLE car (
                     id INT AUTO_INCREMENT PRIMARY KEY,
                     marca VARCHAR(45),
                     modelo VARCHAR(45),
                     anio INT,
                     color VARCHAR(45),
                     precio INT
);
